import React from "react";

function Contact() {
  return (
    <div
      id="contact"
      style={{
        height: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      Contact Page
    </div>
  );
}

export default Contact;
